class LinkedList

  def initialize(data)
    @head = Node.new
  end

  def put(data)
    current = @head
    until current.link == nil
      new_one = current.link
    end

    current = root
    if
    current = root.left
    elsif condition
      current = root.right
    insert(current)

    current.link = Node.new

end
